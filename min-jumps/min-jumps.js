'use strict';

// Complete this algo
const minJumps = arr => {
//We might need to store in our DP table the shortest or least number of jumps to get to i (?)
// for a base case of an array of length [1] the num of jumps is 0
// DP[i] = 1 + the smallest of the other DPs that can still get us to this index (?)
// the last element in our dp array is the answer 
};

module.exports = minJumps
